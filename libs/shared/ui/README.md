# Shared UI

`shared-ui` contains Projects-local, domain-neutral presentation primitives, currently the accessible startup loading overlay. Product feature behavior does not belong in this library.

Public imports use `@pulso-projects/shared-ui`.

Run `npx nx test shared-ui` for focused tests.
