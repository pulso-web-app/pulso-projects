# Projects Placeholder Feature

`projects-feature-placeholder` owns the current routed placeholder and its `PROJECTS_ROUTES` contract. Its explicit name communicates that project-management behavior, persistence, and domain rules are not implemented yet.

Replace this project with intentionally named feature libraries as approved capabilities arrive; do not grow a permanent catch-all feature.

Run `npx nx test projects-feature-placeholder` for focused tests.
