## 1. Placeholder Feature

- [x] 1.1 Add the standalone Projects placeholder component and verify its focused unit tests cover the heading, message, semantics, and absence of false actions
- [x] 1.2 Route the remote root to the placeholder and verify the route activates through the existing `./Routes` contract
- [x] 1.3 Consolidate the loading overlay on its English external template and stylesheet and verify its accessibility test passes

## 2. Validation

- [x] 2.1 Update standalone Playwright coverage and verify the placeholder replaces the loading state on port 4202
- [x] 2.2 Run strict OpenSpec validation and `npm run check`, then verify the route in the Shell when the host is available
